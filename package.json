{
  "name": "vue-find-usages",
  "displayName": "Vue: Find Component Usages",
  "description": "Find all usages of a Vue component across your workspace (template tags, imports, and registrations).",
  "version": "0.1.7",
  "publisher": "ty-ler",
  "license": "MIT",
  "icon": "images/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/ty-ler/vue-find-usages.git"
  },
  "bugs": {
    "url": "https://github.com/ty-ler/vue-find-usages/issues"
  },
  "homepage": "https://github.com/ty-ler/vue-find-usages#readme",
  "engines": {
    "vscode": "^1.75.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "vue",
    "component",
    "references",
    "find usages",
    "cursor"
  ],
  "main": "./out/extension.js",
  "activationEvents": [
    "workspaceContains:**/*.vue",
    "onLanguage:vue"
  ],
  "contributes": {
    "commands": [
      {
        "command": "vueFindUsages.findUsages",
        "title": "Configured Filters",
        "category": "Vue: Find Component Usages"
      },
      {
        "command": "vueFindUsages.findTemplateUsages",
        "title": "Template Usages Only",
        "category": "Vue: Find Component Usages"
      },
      {
        "command": "vueFindUsages.findImportUsages",
        "title": "Import Usages Only",
        "category": "Vue: Find Component Usages"
      },
      {
        "command": "vueFindUsages.findRegistrationUsages",
        "title": "Registration Usages Only",
        "category": "Vue: Find Component Usages"
      },
      {
        "command": "vueFindUsages.findAllUsages",
        "title": "All Component Usages",
        "category": "Vue: Find Component Usages"
      },
      {
        "command": "vueFindUsages.indexProject",
        "title": "Vue: Index Component Usages (rebuild)"
      }
    ],
    "submenus": [
      {
        "id": "vueFindUsages.findUsagesSubmenu",
        "label": "Vue: Find Component Usages"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "submenu": "vueFindUsages.findUsagesSubmenu",
          "when": "resourceExtname == .vue",
          "group": "navigation@99"
        }
      ],
      "explorer/context": [
        {
          "submenu": "vueFindUsages.findUsagesSubmenu",
          "when": "resourceExtname == .vue",
          "group": "4_search@1"
        }
      ],
      "vueFindUsages.findUsagesSubmenu": [
        {
          "command": "vueFindUsages.findUsages",
          "group": "1_filter@1"
        },
        {
          "command": "vueFindUsages.findTemplateUsages",
          "group": "1_filter@2"
        },
        {
          "command": "vueFindUsages.findImportUsages",
          "group": "1_filter@3"
        },
        {
          "command": "vueFindUsages.findRegistrationUsages",
          "group": "1_filter@4"
        },
        {
          "command": "vueFindUsages.findAllUsages",
          "group": "1_filter@5"
        }
      ],
      "commandPalette": [
        {
          "command": "vueFindUsages.findUsages"
        }
      ]
    },
    "configuration": {
      "title": "Vue: Find Component Usages",
      "properties": {
        "vueFindUsages.include": {
          "type": "string",
          "default": "**/*.{vue,js,ts,jsx,tsx,mjs,cjs}",
          "description": "Glob pattern for files to search for usages."
        },
        "vueFindUsages.exclude": {
          "type": "string",
          "default": "**/{node_modules,dist,.git,.nuxt,.output,coverage}/**",
          "description": "Glob pattern for files/folders to exclude from the search."
        },
        "vueFindUsages.includeTemplateUsages": {
          "type": "boolean",
          "default": true,
          "description": "Report component tags and literal dynamic components in Vue templates."
        },
        "vueFindUsages.includeImports": {
          "type": "boolean",
          "default": true,
          "description": "Report static and dynamic imports of Vue components."
        },
        "vueFindUsages.includeRegistrations": {
          "type": "boolean",
          "default": true,
          "description": "Report components registered in Options API components objects."
        },
        "vueFindUsages.componentExtensions": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            ".vue"
          ],
          "description": "File suffixes treated as Vue single-file components. The component name is derived by stripping the matching suffix, so add e.g. \".ts.vue\" for type-checked SFCs (list every suffix you use, including \".vue\"). Also ensure vueFindUsages.include matches these files."
        },
        "vueFindUsages.indexOnOpen": {
          "type": "boolean",
          "default": true,
          "description": "Index all component usages once when a project opens, so the CodeLens and Find All References resolve instantly. Turn off to scan lazily on demand instead."
        },
        "vueFindUsages.parallelIndexing": {
          "type": "boolean",
          "default": true,
          "description": "Parse files across worker threads when building the index on large projects. Disable to index on a single thread."
        },
        "vueFindUsages.codeLens.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Show a CodeLens with the usage count above <template> in each .vue file."
        },
        "vueFindUsages.references.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Contribute Vue component usages to 'Find All References' (Shift+F12)."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "vsce package",
    "publish:vsce": "vsce publish",
    "publish:ovsx": "ovsx publish",
    "publish:all": "npm run compile && vsce publish && ovsx publish"
  },
  "dependencies": {
    "@babel/parser": "^7.24.0",
    "@babel/traverse": "^7.24.0",
    "@babel/types": "^7.24.0",
    "@vue/compiler-dom": "^3.4.0",
    "@vue/compiler-sfc": "^3.4.0"
  },
  "devDependencies": {
    "@types/babel__traverse": "^7.20.5",
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.75.0",
    "@vscode/vsce": "^2.24.0",
    "ovsx": "^0.9.0",
    "typescript": "^5.4.0"
  }
}
