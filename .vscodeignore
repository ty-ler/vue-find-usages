.vscode/**
src/**
**/*.ts
**/*.map
tsconfig.json
.gitignore
test-fixture/**

# icon source (only the PNG ships)
images/icon.svg

# never package scripts, secrets, or build artifacts (match nested paths too)
**/*.sh
**/.env
**/.env.*
**/*.vsix
vue-find-usages-*/**

# archives / build leftovers
**/*.zip
vue-find-usages-*.zip
